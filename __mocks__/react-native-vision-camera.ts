export const Camera = () => null;
export const useCameraDevices = () => ({ back: { id: 'mock' } });
export const useCameraPermission = () => ({ hasPermission: true, requestPermission: async () => true });
